{
  "name": "entrenamiento-m3-s3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "json-server db.json & npx browser-sync start --server --files \"*.html, *.css, *.js\"",
    "dev:db": "json-server --watch db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4"
  }
}
